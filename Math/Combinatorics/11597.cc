#include <cstdio>
#include <cmath>
#include <cstring>
#include <algorithm>
using namespace std;
#define REP(i,a,n) for(int i=a;i<n;i++)

int main() {
	int n,CN=1;
	while(scanf("%d",&n),n) {
		printf("Case %d: %d\n",CN++,n/2);
	} exit(0);
}
